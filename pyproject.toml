[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "comfycolab-video-pack"
version = "0.1.0-dev1"
description = "Optional LTX and MiniMax H3 video generation nodes and workflows for ComfyColab."
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [
  { name = "ComfyColab contributors" },
]
dependencies = []

[tool.setuptools]
packages = []
